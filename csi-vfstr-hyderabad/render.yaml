services:
  - type: web
    name: csi-vfstr-hyderabad
    runtime: node
    plan: free
    buildCommand: npm run build:prod
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: JWT_SECRET
        generateValue: true
      - key: ADMIN_PASSWORD
        value: csi@vfstr2026
